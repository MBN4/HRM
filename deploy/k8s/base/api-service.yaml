apiVersion: v1
kind: Service
metadata:
  name: hrm-api
  namespace: hrm
  labels:
    app.kubernetes.io/name: hrm-api
spec:
  selector:
    app.kubernetes.io/name: hrm-api
  ports:
    - name: http
      port: 3001
      targetPort: 3001
