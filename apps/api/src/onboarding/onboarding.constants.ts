/** `entityId`-adjacent constant only — Onboarding starts NO workflow instance of its own; it only reacts to Offer/workflow events. See docs/conventions/recruitment-lifecycle.md. */
export const ONBOARDING_PROCESS_TYPE = 'ONBOARDING' as const;
