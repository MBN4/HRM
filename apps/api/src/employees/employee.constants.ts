/** The `entityType` this module registers with 0.9's custom-fields framework and (incidentally) with the audit log's generic entity naming — see docs/conventions/employee.md. */
export const EMPLOYEE_ENTITY_TYPE = 'Employee';

export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE = 100;
