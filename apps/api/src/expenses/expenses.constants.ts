/** `entityType` this module starts its sign-off `WorkflowInstance` under — see docs/conventions/operations-modules.md. Literally the SAME entityType workflow.e2e-spec.ts's own reference conditional-amount fixture already uses. */
export const EXPENSE_CLAIM_ENTITY_TYPE = 'EXPENSE_CLAIM';
