# Base URL of the NestJS API. The vendor console talks ONLY to the
# @PlatformRoute() surface (no tenant subdomain/header at all) — see
# docs/conventions/vendor-console.md.
NEXT_PUBLIC_API_URL=http://localhost:3001

NEXT_PUBLIC_APP_ENV=development
